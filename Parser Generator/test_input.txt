/* 1. Standard Expressions (Still supported) */
x = 10 + 5 * 2; 

/* 2. Simple If Statement */
if (x > 15) 
    y = 1;

/* 3. If-Else with Compound Statements (Blocks) */
if (y == 1) {
    a = 100;
    b = 200;
} else {
    a = 0;
}

/* 4. Nested If-Else (Dangling Else Test) */
if (x > 0)
    if (x < 100)
        status = 1;
    else
        status = 2; /* This else should attach to the (x < 100) if */

/* 5. Lexical Error: ID starting with number (Should print error but continue) */
99error = 10;

/* 6. Syntax Error Recovery in a block */
if (a > b) {
    z = 10 + ;  /* Error here */
    valid_after_error = 1; /* This should still parse because of yyerrok */
}

/* 7. Final check */
done = 1;